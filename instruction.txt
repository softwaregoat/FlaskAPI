1. make sure if you made post 8080 with custom tcp for the security of AWS instance
2. login with putty
- sudo apt-get install
- sudo apt-get install python3-pip
- sudo pip3 install flask
- sudo git clone <git directory>
- cd <git-project>
- sudo chmod -R 777 *
- python3 app.py
- open ip:8080
